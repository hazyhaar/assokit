package admin

// DonationStatBox : box stat individuelle pour le header dashboard.
templ DonationStatBox(label, value string) {
	<div class="stat-box">
		<div class="stat-label">{ label }</div>
		<div class="stat-value">{ value }</div>
	</div>
}
